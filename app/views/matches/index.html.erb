<div class="container">
  <div class="row">
    <div class="panel panel-primary">
      <div class="panel-heading">
        <h3>Your Matches</h3>
      </div>
      <div class="panel-body">
        <div class="container">
          <div class="row">
            <% @studentmatches.each do |match|  %>
              <div class="col-md-10">
                <h4><%= match.day %> | <%= match.first_student.name %> & <%= match.second_student.name %></h4>
                <hr>
              </div>
              <% end %>
         </div>
       </div>
     </div>
   </div>
 </div>
</div>
